# week-2-todo-list-sharedpreferences
To‑do list app in Flutter using setState for state management. Tasks saved locally with SharedPreferences (persist after app restart). Features: add tasks, display in ListView, auto‑load saved tasks. Covers counter app exercise as foundation for data persistence.
